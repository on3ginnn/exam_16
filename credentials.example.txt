Демо-входы (после migrate + loaddata или при использовании приложённой db.sqlite3):

Администратор медиатеки (is_staff)
admin / pass12345

Обычный пользователь
user1 / pass12345

Скопируйте файл в credentials.txt при необходимости (credentials.txt в .gitignore).
